// Stoa — minimal ESLint flat config (ESM).
// This is intentionally permissive: it provides a valid baseline that
// type-aware TypeScript rules (e.g. @typescript-eslint, typescript-eslint
// flat presets) can be appended to as the monorepo's lint story matures.
//
// To add TS rules, install `typescript-eslint` and spread its configs here,
// e.g. `import tseslint from "typescript-eslint"; export default [...tseslint.configs.recommended];`

export default [];
