{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@11.3.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @stoa/web build",
  "outputDirectory": "apps/web/.next"
}
