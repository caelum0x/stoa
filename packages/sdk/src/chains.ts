// @stoa/sdk — re-exported chain primitives.
//
// Convenience surface so SDK consumers do not need to depend on @stoa/skills
// directly for chain/config constants.

export {
  pharosAtlantic,
  pharosTestnet,
  DEFAULT_CHAIN,
  TEST_USDC,
} from "@stoa/skills";
